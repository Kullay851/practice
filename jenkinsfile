node ('RHEL,'){
    stage ('SCM'){
        git 'https://github.com/Kullay851/game-of-life.git'
    }
    stage('build'){
        sh 'mvn package'
    }
    stage('post build'){
       junit '**/TEST-*.xml'
       archive '**/*.war'
    }
}

